{
  "name": "claude-note",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin: generate a daily work note from transcripts",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test 'test/*.test.mjs'"
  }
}
